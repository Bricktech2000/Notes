# Linked List

## representation

### Singly Linked List

```mermaid
graph LR
  A(HEAD) --> B(item) --> C(item) --> D(item) --> E(TAIL)
```

### Doubly Linked List

```mermaid
graph LR
  A(HEAD) --> B(item) --> C(item) --> D(item) --> E(TAIL)
  E --> D --> C --> B --> A
```
